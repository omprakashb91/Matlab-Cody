% Problem 10.
% Determine whether a vector is monotonically increasing

function tf = mono_increase(x)
for p=2:length(x)
    if x(p)>x(p-1)
        tf=true;
    else
        tf=false;
    break
    end
end

end
